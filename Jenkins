pipeline {
  agent any

  environment {
    TF_VAR_aws_access_key = credentials('aws-access-key-id') // AWS_Jenkins credentials
    TF_VAR_aws_secret_key = credentials('aws-secret-access-key') // AWS_Jenkins credentials
  }

  stages {
    stage('Checkout') {
      steps {
        git url: 'https://github.com/jyothi0301/terraform-ec2.test.git', branch: 'main'
      }
    }

    stage('Terraform Init') {
      steps {
        sh 'terraform init'
      }
    }

    stage('Terraform Plan') {
      steps {
        sh 'terraform plan'
      }
    }

    stage('Terraform Apply') {
      steps {
        input message: 'Approve EC2 creation?'
        sh 'terraform apply -auto-approve'
      }
    }
  }

  post {
    always {
      echo 'Pipeline completed.'
    }
  }

}

